packages:
  - 'packages/*'
  - 'services/*'
  - 'apps/*'

# Disable pnpm 11 auto-rerun of `install` before every script — it would
# re-emit ERR_PNPM_IGNORED_BUILDS and crash turbo. We trust the lockfile.
verifyDepsBeforeRun: false
